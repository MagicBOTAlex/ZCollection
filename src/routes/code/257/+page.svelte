
                <script>
	const snippet = String.raw`                Here is a sample implementation of a Svelte component that mimics the classic "drag and drop" interface. This implementation assumes Svelte 5.

svelte
<svelte-component name="DropBox">/null!</svelte>


And here is the implementation code:

svelte
<svelte-component name="dropbox">/SvelteKit.drag

<s .class="drag" @click="myTarget = 'myTag'">
    <div class="text" @text="red">The red text</div> 
    <div class="text" @text="blue">The blue text</div>
</s>


This component has a drag and drop interface. The name element has an attribute called @class, so if the user is typing out their "Drag and Drop" interface, they can do it like this:

svelte
<s .class="drag" @click="myTarget = 'myTag'">Dropbox</s .class>
<div class="text" @text="blue">
    The red text
</div>

<scrip.init-->
 
< s
  data: { 
    dropbox: SvelteAppKit. dragDropBox; 
},


This S component is written in Svelte, version 5 and above.

If the user has typed "Drag and Drop" interface", it can do this:

svelte
<s .class="drag" @click="myTarget = 'myTag'">
  <div class="text" @text="red" >
    The red text
  </div>
</s>


The S component has a drag and drop interface. The @ attribute calls the Dropbox component, which is an Svelte "drag and drop" interface.
                `;
</script>

{snippet}
                