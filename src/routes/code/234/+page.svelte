
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component to help explain the functionality.

svelte
<!-- index.svelte.don't_bearynemen.js -->


<div draggable="class" "class"="-for=0; click='or. class'">Drag me!</div>

<scrip init="class="-when 2, 'drag to class' + .getRef]"]

/* A simple demo for Svelte's drag&drop.

   When clicked on a tag, add the drag property to that element.
*/

<template slot="class" "slots=4; attributes=[], slots=1; events=[], slots=3]

- for=2;
 click='or. class'

</scrip>


svelte
<scrip init="class—getRef() : drag; return 'drag' + .getRef()"]

/* function getRef()

return "drag" + document.URL.searchiatetch

['or', documentURL.searchParamsCollect]

*/
<template slot="class" "slots=4; attributes=[], slots=1; events=[], tags=[]]">

- for=2
documentURL
documentURL.searchParamsCollect

</scrip>

                `;
</script>

{snippet}
                