
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a "drag and drop" system using the framework:

svelte
import { makeObservable } from sveltejs/stk;
from SvelteKit.SvelteKitSvelte;
use SvelteKit.Draw.Paste;

app -> "Drag and Drop System" {
  create an instance
  | "Drag And Drop" (new "App Component" => 1)
{
    create a paste with new component
}

  after () -> {
    // do something when the component is created
  | "Create Paste",
    new SvelteKit.PasteComponent("New", "Component")
}


html
<scrip type="x-matrix-dnd"]>

<svg data-attribute="drag" draggable="this">Drag</svg></scrip>


svelte
// component creation

create an instance

create a paste with new component
end


svelte
// initial state and setup

import { ScreenComponent, build } from 'SvelteKit.Draw.Paste';

// setup state
// always update the component's initial state
create a drag element (new "App Component" => 1)
{
    // add an event to the paste component
    new SvelteKit.PasteComponent("New", "Component")

// component body

</p>

<!-- Initial State -->
<svelte-viewport class="drag">
  <svelte-dot-dots>
    { @, ScreenComponent }
</svelte-portion>
                `;
</script>

{snippet}
                