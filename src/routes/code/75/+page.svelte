
                <script>
	const snippet = String.raw`                Here is a sample implementation of a "drag and drop" system in SvelteKit.js:

svelte
import { ComponentManager, Controller } from '@svelte-k kit';
import { VueComponentManagerSvelte Kit} from '.js/commands/dragTab';

const tab = (root) => {
  root.create('DragDrop', 'A bit of drag and drop.');

  var store;

  function init() {
    if (!tab.0()) {
      return;
    } else {
      tab.1();
    }
    tab.2();
  }

  init();

  tab;
};

svelte k;


html
<svelte-3k="tab">Drag drag</svelte-k</svelte-2>


javascrip
// The initial state of the component

var store = (root) => {
  root.create(() -> 'A bit of drag and drop'), 'create' + "initial";

  var action = 'update';
  var actionName = 'title';

  return action;
};
svelte k["dragAndDrop", { id: "m", name: "" }]);

// The first update

var store = (root).ref['0'].create(() -> . "update a bit of drag and drop"), "drag" + "Drag";

svelte k["drag", { id: "drag"}], actionName, action); 


Note that SvelteKit.js does not have the V$ keyword for creating ComponentManagerSvelte. Instead it uses a more linear way with state management.

This component is written similar to many other drag and drop systems found in Svelte 4. However, as this example demonstrates, SvelteKit (or its older version) has different ComponentManagerSvelte implementations when compared to Svelte 5 or Svelte 3.
                `;
</script>

{snippet}
                