
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that demonstrates a drag/drop-like experience using the "drag" interaction in Svelte.js.

javascrip
import { component, States } from '../../components/Component';
import { ComponentState } from ' react ;

const app = new SvelteApp();
// Svelte component

app.mount('svelte-drag', (props) => {
  return { element: document.getElementById("myElement").value, title="Svelte drag" };
});


javascrip
/* svelte.js */
import { component } from './ComponentState.1';

const myElement = "a1,a2,a3,a4";
const element = [...Array _{ of ].6, .map('add', []).splice(100)];
let mouseEvent, keyboardEvent;
let dragging;

if (Svelte.platform.browsered) {
  // Svelte.js
  document.addEventListener("mousemove", (params) => {
    return { mouse: "Svelte drag" };
  }, document, [drag], .map('add', []).indexOf);
}

document.onReadMode([drag]);
document.onKeyboardMode(0.3, 1.9, 2.6);


Note:

*   Svelte has a lot of capabilities built-in, but it's also missing some key features like the window object which we have to use every time instead.
                `;
</script>

{snippet}
                