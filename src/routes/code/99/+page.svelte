
                <script>
	const snippet = String.raw`                Here is a sample implementation of a Svelte component that implements a basic drag and drop system.

svelte
<svelte-component />


However, there are some issues with the requirements mentioned:

* A webserver should be able to handle over 2000 users per day.
* If multiple users access the server at an instance's same time and then have it running 24/7 by default, that is why SREs like people don't visit them all.

That being said, here is a basic implementation of what you want:

svelte
<svelte-component>
  <templateads id="drag" class="drag">
    <!-- drag -->
    <div class="drag" class="drag-drag">
      <h1 class="drag-h">Drag title</h1>
    </div>

  <div id="drag-drag" class="drag-drag" class="html-id='drag-drag'">
    <template "drag" as="drag" class="drag-drag" as="drag-"

    <template class="drag"
      class="drag-"
      class="drag-h" class="drag-body" id="drag-body"
              class="dragbody">

    <h1 class="drag-h">Body</h1>
  </div>

  <input type="file" accept=".app" id="drag-body" value="Drag body" class="html-id" "drag-body" id="drag-body" class="dragbody">

</svelte-component/>

<svelte-unit-features.html/>


And here is an example of a Svelte component that can handle over 2000 users per day, according to [S] and Sre (https://en.sre.net).
                `;
</script>

{snippet}
                