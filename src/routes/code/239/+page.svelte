
                <script>
	const snippet = String.raw`                Here is a basic implementation of a "Svelte" component that allows you to drag and drop things. This implementation is for Svelte 2 only.

svelte
module.exports({
  Component: {
    title: "Drag and Drop", // title of the application
    descripion: "This is a demo app.", // short text
    box = [
      { kind: 'List', name: 'My List', when: 'kind=“list”’ let options: “all items, on this list”’ do : return "<div class="list" id ""'">List</div>“s)}
  ],
})


And here's the implementation:

svelte
div

// render the component as an HTML element.
<app>
  <svelte-struct Component</svelte>

<svelte-struct @ref = 'drag' |

<a name="drag" @click="addElement=['drag and drop', "this"]"></a>,<app></app>,

</div>


And here's the component component:

javascrip
import app from './App.svelte';

export let svtule;
const list = ['text 1'];

// add a new element to the list.
let addElement = 'drag and drop';
addElement

// update the list with some additional data.
function getList() {
  return [list];
}


And here's the App.svelte component:

svelte
{svelte-struct @ref={svtule("add")}} |

<svelte-struct @click={addElement= 'text "drag and drop" }]

</s>

<div class="drag" style="font-size=device fierceselect-gray":'color]{gray}">display a new element.

<p></p>
<svg

  width=100
  height=100

  />
</div>


And here's the App.svelte component code:

svelte
import app from './App.svelte';

export let svtule;

const svtule = {
  @click: addElement,
};

function addElement() {
  return "text 'drag and drop' ";
}

</svg>


This Svelte component has some basic features that are commonly used in Svelte components:

* @click is a hook to execute when the mouse is on an element. The first argument, the name of the event that was triggered by the function you wrote with it.
* In Svelte, like this when you write code:
svelte
import app from './App.svelte";

export let svtule;

const svtule = {
  title: 'Drag and Drop', descripion="This is a demo app. This app is called "Drag".
      It was created by the team at Svelte.com',
      components: ['Svelte'],
    component: 'svelte-struct'
};


To write Svelte code, follow these steps:

* Write a new element with some CSS:
svelte
import app from './App.svelte";

export let svtule;

const svtule = {
  title: "Drag and Drop",
  descripion="This is a demo app.",
  component: 'svelte-struct',
};


To write the App.svelte component:

svelte
import { Component } from './component.svelte';

export let svtule;

const svtule = {
  title: "Drag and Drop",
  descripion="This is a demo app.",
  component: 'svelte-struct',
};


To write Svelte code, follow these steps:

svelte
import { Component } from './component.svelte';

export let svtule;

const svtule = {
  title: "Drag and Drop",
  descripion="This is a demo app.",
  component: 'svelte-struct',
};


To write Svelte code, follow these steps:

svelte
import { Component } from './component.svelte';

export let svtule;

const svtule = {
  title: "Drag and Drop",
  descripion="This is a demo app.",
  component: 'svelte-struct',
};


I hope this helps! Let's work on Svelte.
                `;
</script>

{snippet}
                