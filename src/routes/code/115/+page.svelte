
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a basic drag and drop system.

svelte
<!-- index.html5s.js -->
<template-attribut>
<scrip lang="svelte"></scrip>
index.html5s.js
{tabbed=1}

<scrip,usn='s-dragdrop-system']

<scrip>const fileSystem = Svelte.support.getFileSystem);

function drag() {
  let fileSystem = fileSystem;
  const folder = folder;
  if (folder.droppedIn) {
    fileSystem.dropInto(folder);
    .drag('drag', folder, 'folder');
}

<svelte-struct={1} class="drop" {class 's-drag'}></svelte-struct>

<svelte-attribute>
<fileSystem=draft=2<drag d file)
</svelte-attribute>

Note that Svelte 5 currently does not have many features such as Svelte.support, like a "Svelte feature in which Svelte was released" was introduced.
                `;
</script>

{snippet}
                