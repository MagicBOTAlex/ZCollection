
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a drag and drop system. This component will allow users to be able to open the "drag" modal.

svelte
import { makeComponentName } from './utils;
export default inedit.DragComponent;
inedit.DragModal;
outlet e = 'Enter a name'
  when new {
    const current = "Drag"
      if (window dragging) {
        console.log("Drag link activated")
      }
  while (
    !window document.display('drag')
            && document.querySelectorAll(".drag", document.body).length
            > 2
          )
  // <button type="link">Link</button>
// <div type="attribute-const">data</div></button-type>, {
  'class' => e,
  'id' => e
}


Note that Svelte can also be run as a global application.

svelte
export default app;

app = "edit the following output"

However, Svelte does not have a comprehensive array of components. It has an array of key-value data and a js-console with a js-update feature to create a browser-based experience, which may be more convenient for some people.
                `;
</script>

{snippet}
                