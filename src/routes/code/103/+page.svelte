
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component that adds a drag-and-drop feature to the application.

dart
// components/drag.jsm.dart

class Drag({
  @private String draggingItem = "dragging item"
),

// drag/dragging_item
@override,
class $0 => {
{
    $refState = "ref of";
    ref('drag&drop', "name, name")

} },
@export. drag();


svelte
import { svelteX } from '@builds/;

const div = () => document.createElement('div', 'Div')

svelte.x('drag-box')
  $(ref draggingItem)
  $ref
    .dragDrop("Name, Name")
  .link(
    {
      // Add a link to the drag box
      "Name" => "name of name",
    }, (Name) => String);


html
<div class="drag-box" id="drag-box">Drag-Box</div>

<m>Hey there!</m>
<text Box</display></textBox>
<m></div>, [ref ' drag box'] ],
[ref 'name box']],
<div id="drag-box"></div>,
<a draggable="Name" for="Drain a name" href="drain a".html id</a></div>



This component is identical to the first Svelte component in this question, which implements dragging. If you want to add any additional drag-and-dropping features, just replace the following lines:

svelte
import { svelteX }

const drag = ref() => "drag of name, name"


To implement adding drag boxes:

html
<div class="drake"></div>,
[a.addEventListener "Name",]
action(), [ref 'dname']
  <a id="drakename">DRAG-BOX</a>

</div>


If you want to add a link to the name box, you can't. But if it's not too much to ask (and in most cases just a few lines of code), here is how you do it:

svelte
import { svelteX }

const drag = ref() => "Drag of Name, Name"

add(drag)


If someone else wants to make a Svelte component that adds drag boxes and if they're ever asked why I add these components it's because adding is done by Sapper.
                `;
</script>

{snippet}
                