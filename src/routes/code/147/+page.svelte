
                <script>
	const snippet = String.raw`                Here is an example of how you could create a Svelte component that allows dragging and dropping into a list item. I'll also explain how to use it.

svelte
<scrip lang="s".dropdown")]
import ListItemComponent = ListItems:1;
ListItemComponent("drag", "drop") | (x: x) {
  if (x) "drag" {
    "title" = "Descripion: Drag and drop";
  } else "content"
}


greet
</svelte>

<scrip,lang="s".dropdown")]
import ListItem:1;

ListItem.$("open") | list | { text : "Drag and Drop", title : "Example with Title" }} {
  $(x) "title"
}


You can use the following code in SvelteKit.

javascrip
import List from '@svelte/kit/base';

const list = new List();

list.$("open") | (title: "Example with Title"), { drag: true }}


This is how you would implement a Svelte component.
                `;
</script>

{snippet}
                