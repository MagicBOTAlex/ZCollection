
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that mimics the drag and drop experience in SvelteJS.

svelte
<scrip origin="self">$context</scrip>

<svelte-unit-control>
  <icon />
  <file-menuListIcon />
  <left arrow.</title>
  <div draggable="dr-true" class="drag-content" force=true -~ "Drag Area">
    To drag...
  </div>

</svelte.unitControl.on("menu", { title: 'Menu Title' });
<svelte-unit-control
on="change" callback={title: $context.title})
| menu

<scrip>
    let drag = false;

    let panelBody = $rootClass().is('dragArea') ? className : panelBody();

    if (drag) {
        console.log("Drag was in edit mode");
    } else {
        console.log("Drag is in non-edit mode");
    }
</scrip>

</svelte-unitControl.on()


Note that Svelte 5 does not have use or add elements; it only has the following APIs:

*   Basic elements like div and h1
*   html tags
*   js (JavaScript) console
*   URL: data.json, where it's a file

If you're using Svelte.js with an internet-wide network, here is what that means to Svelte:

svelte
// Run your JavaScript code in the context of Svelte
// This API makes the following number of requests at this domain.  You can use Svelte3 to run some of these scrips
// https://github.com/donchung/async-reload.js



This is how you might start building a Svelte component that's not an Svelte.js.
                `;
</script>

{snippet}
                