
                <script>
	const snippet = String.raw`                Here is an example of how to create a Svelte component for a drag and drop system.

dart
// drag_and_drop.dart
import "svelte3/vesselscope.js" sound;
var vssound;

static {
    vssounds.add("File", listenAndLoad);
}

void listenAndLoad(String fileName) {
    // Do something specific with the specified name,
    // or a function that can be expressed in JavaScript.
    System.log("Drag and Drop System");
}


svelte
// drag_and_drop.svelte

import "vssounds.js";

System.log("Drag and Drop System");

svelte.scope = 'File';

var file;
file?.load;

function loadFile(){
  file ? openFile
};


This Svelte component defines a vssound that can be added to the application. The listenAndLoad function is called when vssounds.add('File', listenAndLoad). and is triggered by a Svelte "sounding" sound.

svelte
// drag_and_drop_s.svelte

import "vssounds.js".system.log;

System.log("Drag and Drop System");


You can now use this component in your application like this:

dart
// main.js

import "main.js" system log";

system log;


That's it!
                `;
</script>

{snippet}
                