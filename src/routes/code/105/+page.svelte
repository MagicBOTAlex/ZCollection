
                <script>
	const snippet = String.raw`                Here is an example of how to create a Svelte component for a drag and drop system.

snetlet
import { makeSvelteComponent } from 'sveltejs-component';
import { createElement } from '$helpers';

const DragDrop = {
  name: "DragAndDrop",
  descripion: "A basic Svelte component that can be extended.",
  version: [
    createElement("hover,select"),
    createElement("copy")
  ],
  createComponentOnMount: () => {
    if (arguments.0) return;
    if (arguments.1) return null;

    const components = ["div", "input", "button"];
    for (const component of components) {
      if (arguments.2) || arguments.3) || arguments.5) {
        if (arguments.4) ? component.versions.create["drag,drop,active"][6] : 10) return null;

        createElement("SvelteComponent", "name", "DragAndDrop"),
          { name: "DragAndDrop" },
            .render(function: null) {
              if (arguments.1)) || arguments.3) ? component.versions.create["drag,drop,main"][6] : 10) return null;
            };
            .mount(() => {}, [1])[
                .catch(function, 0, 1) { isDisabled: 2 } .version(6)];
        }
    },
    7
};


This component consists of multiple components and elements. It also includes some methods.

html
<div name="drag" for="Drag">Drop</div>
<scrip type="module" src="scrip.js">createComponent</scripType</svelte>


The code above is a Svelte component with 5 features, but it's Svelte 5 instead.
                `;
</script>

{snippet}
                