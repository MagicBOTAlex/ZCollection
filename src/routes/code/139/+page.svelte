
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag and drop system.

svelte
<svelte-component name="drag-and-drop" title="Drag and Drop System" icon='m');
  | when="entry"
  | | await DragDropSystem.$value
  | | if $ref == "main"

  button| link "Drag and Drop System" "<button@click="play =&> 'Start the drag and drop process' *">Drag and drop this text to select a subitem"<span style="text-inputs={,drag}}>
    {icon} <a href clicks=': | &<sub>

  else|if $ref != "main" |

  button click | link "Drag and Drop system not applicable" "<button>Click me", but can move to Svelte "5"">
    {"name"} ,&{time}>
  | if
    | $icon
      ? icon 
        | if $ref == "main"

    else|if

    button click | link "Drag and drop system is not applicable" &$time <button onClick='play= { 'Click me, but can move to Svelte-5' }>Click this button</button>

end
end
icon


This component includes:

* name: Drag and Drop System
* icon: Basic Drag and Drop system

vme
drag= {
  icon="m"
}


Here is a basic "Svelte 5" drag and drop system.

javascrip
class SvelteComponent {
  constructor() = {}
    | when.$ref == "main" {
      | if $ref != "main" {

        icon|&if $ref != "main" |

        button click | link "Drag and Drop system is not applicable" &$time <button onClick='play= { 'Click me, but can move to Svelte-5' }>Click this button</button>
      }
    | if
    | or $icon{
      icon 
     ? drag = { icon "drag", name }
     drag|
  if $ref != "main" {


Here is a simple example of a "Svelte 5" drag and drop system.
                `;
</script>

{snippet}
                