
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component that allows you to drag and drop different types of items in the application:

svelte
<svelte-component name="Drag Component" text="Click me and add the component"]
  <drag-view id="view">name</drag-view>
  <!-- Note: Svelte 5 doesn't have built-in methods like "add", or for removing a drag view. The drag view is created with the new element, it can be added manually to the existing Drag View by using svelte-detect-if -->
  <button text="Add a component">Click me</button>
  <div text-classes>Drag component</display unit="Drag View" if "Svelte" version >1 {text} dragView}
    <a slink "name" text="Click me and add the name of the item">
      Add a component
    </a>
    <div class="drag-view">
      Drag view: Svelte 5 has built-in methods like svelte-detect-if for creating drag views.
      <button class="remove-drag-View" text="Remove drag view">Click me</button>
      <svelte-kate "name" class="drag-view"]
        name
    </svelte-kate>
  }
</div>

<svenvelope

<div class="drag-view">
  <a slink "text" text="Click me and add the component of the items">
    Click on this area: click me if you are using Svelte 5
    <button class="remove-drag-View">Remove drag view</button>
    remove
  </svelte-kate>

</svenvelope

<svenagram

<div class="drag-view">
  <a slink "text" text="Click me and add the component of the items">
    Click this if me for adding a component of the items
    click here and then go back on the item's drag view
    Remove that element
</svelte-kate>
</svenagram
</svenvelope

<svelloguest

<div class="drag-view">Remove Svelte 5's built-in methods</div>
</svelte-knowledge>



This is a basic implementation. You can add more classes and features to the component as needed.

javascrip
import { View } from './view.js';

export default (class Name) => class dragComponent {
    createView() {
        return 'Drag' + name + 'click me');
    }

    createElement() {
        // When you call svelte-detect-if
        // it's a Svelte 5 app.

        const dragView = new View();

        if (Svelte.current.version > 1.3) {
            class Svelte2;
            class Svelte3;

            if (Svelte3.version < Svelte5.version) {
                var SvelteVersion = Svelte5.version + 1.6 - Svelte2;
            }

            return new View("name", [class Svelte2]);
        } else if (Svelte4.version > 1.8) {
            class Svelte4;

            if (Svelte5.version < 3.0) {
                var SvelteVersion = "Svelte 5" + String(Svelte2).toUpperCase() + '.' + Svelte5.version;
            }

            return new View("name", [class Svelte4]);
        }
    }
}


I hope this helps!
                `;
</script>

{snippet}
                