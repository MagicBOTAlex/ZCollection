
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a "drag and drop" system in Svelte 5:

svelte
<scrip origin="app.com"> origin</scrip>

<svelte-Kit
  class="kit"
  attribute="{dragDropEnabledWorldClass}"
end>


This component includes the following classes:

* drag for the drag functionality
* drop for the drop message

When a user interacts with this component, they can perform actions like saving data (saveDataAction) or applying an action (applyCommand). The Svelte components provided here will also be able to handle these actions.

svelte
function saveDataAction() {
  return "Save Data" + "Applied Command";
}

function applyCommand() {
  return "Apply Command" + "Resulted Resulting";
}


The Svelte components drag and drop provide the following APIs:

* drag.Kit.drag("saveDataAction")
* drag.Kit.drop("applyCommand")

These classes will also have access to other methods in Svelte 5, such as those provided for SvelteKit classes.

svelte
<scrip origin="app.com"] import imports import
  class "drag.kit.drag('saveDataAction')" : "drag.1"
end>


The Svelte component classes will also have access to other methods in Svelte 5, such as:

* drag.Kit.d["drag"].drop("drag.4")
- As of SvelteKit 6.0, the following features are provided for SvelteKit:
+ Classes (classes) provided with SvelteKit
+ Methods (methods)
+ Variables (variables)
+ SvelteComponent (SvelteComponent)
+ drag.1 : "drag.1" + "drag.3"
- drag.4 : "drag.4"

When a user interacts with this component, they can save data and apply an action.

svelte
<scrip origin="app.com"] import imports import "drag.4"
  class "drag.3"


The Svelte component classes will also have access to other methods in Svelte 5, such as:

* SvelteKit.exportForExportName()
+ Class (class) provided with SvelteKit
+ Variables (variables)

svelte
function saveDataClass() {
  return "Save Data" + "Applied Command";
}

return "Apply Command" + "Resulted Resulting"


The component classes will also have access to other methods in Svelte 5, such as:

* SvelteKit.exportForExportName()
                `;
</script>

{snippet}
                