
                <script>
	const snippet = String.raw`                Here is a basic Svelte component that demonstrates how to create a drag and drop system with some features:

svelte
<!-- index.html5.js -->

<!html5js.js>
{
  scripEl(
    "My Drag System", 
    "(drag) file" 
  )
}
else {
  // Create a new drag document.
  var documentEl = documentElement;
  if (documentEl === 'HTMLDocument1') 
  {var bodyEl = documentEl}

var dragSystem
<!-- Basic component -->

// Add custom classes for later use.

<app title="My Component" />
</svelte>


html
drag.html

<scrip.svelte
{
    // Create a new custom element.
    return "Drag System"
}
else {
    // Create a new document editor.
    var elementsEl = documentElement.createElement("DocumentEditor", 1);
    documentElbodyDivEl = documentElement;
    if (documentEl === 'HTML5DHtml5html5.html') {
        return "Custom Element";
    }
    if (documentElbodydivDivEl) {
        documentElbodydivdiv = documentEldiv;
        var elementsEl = documentElEl;
        if (elementsEl) {
            // Create a new document editor.
            var documentsEl = documentElElement;
            if (documentsEl) {
                // Create a new custom element.
                return "Custom Element";
              }
        }
    }

}


html
/* index.html5.js */

<app title="My Component" />
</svelte>


Note that Svelte 5 is often referred to as SvelteKit. However, it's worth noting that the name "Svelte" was changed from "Svelte" to "SvelteKit".
                `;
</script>

{snippet}
                