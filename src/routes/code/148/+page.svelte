
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a drag and drop system:
svelte
<scrip type="module"-as="svelte")]

<div class="drag-drag" aria-hidden="true">
  <div id="drag-area" class="drag-drag"
  	deduct-element="drop"
></div>

<div id="drag-area-class1" class="drag-drag"
	deduct-el="class1",
	class1="select"
></div>
<scrip type="module"-as="svelte"].class = 'Svelted component', 'component Class name' in document, 0.5;
svelte.annotation('Class name');
svelte.tips["Drag and drop"];
</svelte> -->

This code is written in SvelteScript 3 (S5), which is a newer version of the Svelte ecosystem that allows for more complex JavaScript and DOM scrips.

If you are using SvelteKit, the equivalent code would be:
svelte
<scrip type="module" as "svelte-kojs" import="svelte-koj.useSvelteScript);

// .component("drag-drag", "drag-component"))

svelte.ember('class1', { 'select' })

Note that Svelte 5 (and the accompanying SvelteKit) has many new APIs and can use several of them.

If you are using an old browser, please consider using the older version of SvelteKit which is still supported until December 2023.
                `;
</script>

{snippet}
                