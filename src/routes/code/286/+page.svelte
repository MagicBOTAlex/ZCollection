
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that mimics the functionality of a standard "drag and drop" system.

dart
// svelteComponent.js
import SvelteKitVisualKotlin.sveltekit from SvelteKit;

var app = mount(
    Component: 'SvelteKit', { "svelteKit": 1 },
    {
        0, // when the component is being initialized,
     },

    {
    0, // when the component has finished initializing
   () -> {
      var viewport = App.svelte.kit
          .where('id', 'v, sapp', { "svelte", "svelteKit": "SvelteKit".join1(), { id: 'svelte'.join4, "svelte"', "id" }.key },
         0, // when the component has finished initialization,
        {
    },
    0, Dart
)

{
  svelte("svelte", (onclick, self) -> {
    var button = mount(
      Component: 'Svelte',
      { "svelte": "SvelteKit".join1(), "id" }, 
     [0]
    )
    sprels("my Svelte component", .5)
}
 Dart
}


javascrip
// svelteComponent.js
import SvelteKit.sveltekit;

var app = mount(
  SvelteKit.sveltekit,
    {
      1, // when the component is being initialized,
     },
    .0, // when the component has finished initializing,
   0, // when the component has moved away from us,
   { id: 'svelte'.join4, "id" }, 
         1, Dart
)


Note that this code requires SvelteKit modules to be installed. Also note that in SvelteKit, @kotlin module is used with app parameter.

SvelteComponent.sveltekitModule() = "svelteKit"
SvelteComponent.sveltekitModule().key("1") = "a"
                `;
</script>

{snippet}
                