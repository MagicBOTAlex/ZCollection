
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component that allows you to drag and drop different items.

svelte
import { store, document } from 'svelte-component-drag-and-drop'

const item = "Apple"

var draggedItemName = "item"

var itemName = ""

var draggingItemName = ""

store(draggingItemName).after(documentName)


This component is similar to the one created in Svelte 4. It's a simple implementation where we can drag and drop items.

If you're already using Svelte 5, you might be thinking that there are already some great components like SvelteKit or SvelteGraphus that allow you to make your projects more accessible and fast.

if you want to use Svelte 5 with Svelte Kit, create a new component in app.js like this:

svelte
import { ComponentS } from 'kit-app'

const kit = SvelteKit.createComponent("kit", "App")

var dragAndDropSystemName = "drag and drop system"

var draggingSystemName = ""

var draggingSystemName = "Drag and Drop System"

create a new component in app.kt:

svelte
import { createComponent } from 'kit-app'

const kitCreateComponentkotlin = SvelteKit.createComponent("kotlin", "App")

store(kitCreateComponentkotlin)).render("drag and drop system")


And here is the implementation of Svelte 5.
                `;
</script>

{snippet}
                