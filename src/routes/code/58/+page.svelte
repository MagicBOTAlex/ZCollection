
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag-and-drop system.

svelte
<scrip, plain="express" "default"> let myComponent = fork(in app))

app.onMenu={ ->
    console.log("You clicked on the 'View' tab.")
}

<div class="drag-box">
    <button class="hover-button">Click me</button>
    dragItem name="my Name"
    draggable document="yep, that's a simple implementation of a Svelte component")
  </div>

<scrip>let draggingBox = ref("dragging", {x: -10, y: -5, speedPoints: [1]]})
</scrip>
<div class="drag-box">
    <button class="hover-button">Click me</button>
    dragItem name="my Name"
    draggable document="yep, that's a simple implementation of a Svelte component")
    <div class="overlay" @click="openOverlayDialog" button -->
        <div class="close-plane" button
            class="fly-toggle"
              data-color = .blue
               color = "gray" # overlay
                { overlayDialog } =>
        </div>

</div>

<scrip, lang = 5</scrip>, export {
    openOverlayDialog() {
        console.log("Click the 'Open' button on the right.")
}
</scrip>
<div class="overlay" @click="openColorDialogDialog = null , click='red' color='blue'"> Red</button>1
</div>

<scrip, lang = 5</scrip>, export { ColorBox } />


This Svelte component has three states: myComponent (a boolean), a dragBox (an object with an onClick method), and a dragItem (another object). The dragItem has the following properties:

* name: the name of the item.
* text / placeholder: if you're looking for a text or placeholder, e.g.:
    * If you are clicking on a button with no hover elements or click-to-button, but can also open your Svelte component.

The components have three states: myComponent, dragBox, and dragItem.
                `;
</script>

{snippet}
                