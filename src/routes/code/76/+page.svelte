
                <script>
	const snippet = String.raw`                Here is a sample implementation of a Svelte component that allows dragging and dropping into a text input.

svelte
<scrip root Class="DragDropSystem">
  { windowconsole }
</scrip>

<svelte-root App>{{ "drag" }} to {{ "drop" }}>
  <SvelteComponentRef ref={ref = "text input"}}
    value={drift)}
    draggable="true"
    html clickable="yes" true
    arrowLock=0
    dragLock=1
    dragLock=2
  >Drag { .. "data" }</svelte-root>
</scrip>

<scrip root $(ref = "drag"), drag() when updated]


javascrip
const ref = 'drag';
let data = null;
let dataType = 'text input';

if (window.console) {
    console.groupBy(ref);
    console.log(1, 2, 'three');
} else {
    // If the user is currently dragging,
    // we can display a warning
    if (ref) {
        ref.history = 3;
        ref.history.open();
    }
}


This component has four classes: Svelte-component-ref, text input, _ click, and drag. Each class is displayed with the help of Svelte's many modules.
                `;
</script>

{snippet}
                