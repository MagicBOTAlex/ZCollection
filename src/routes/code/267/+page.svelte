
                <script>
	const snippet = String.raw`                Here is a basic example of a Svelte component that implements a drag and drop system.

First, we need to add the following code to our HTML file:

html
<!DOCTYPE NULL>;


svelte
<scrip type="amp-model"
attrs"{$vm.drag.drop}['drop']" onDragStart="dragEvent" onDragMove="handleDrag"]
class Drag {
  emits(["actionName"])
}
scrip


And here is the code to create a component that can drag and drop:

html
<!-- index.html -->

<template-attribues
.attrs({ { $vm.drag.drop } [0], $vm.drag.speed] })
</scrip,

                `;
</script>

{snippet}
                