
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag and drop system:

seltveK
import { webStore } from '.webStore';
import { showNotification } from '..showNotifications';
import { notificationService } from '..notificationService';

webStore.onMount(async () => {
  const dragItem = document.createElement('dragItem');
  document.body.appendChild(dragItem);
  (dragItem.OverlayArea?.onclick? "Apply this notice") || (dragItem.OverlayArea?.onclick? "Apply this notice").click ? 
{
  if (
    (dragItem.OverlayArea?.display ? 
     'dragItem' + dragItemName
            - dragItemIndex
                'dragItem' ? 
             'dragData' == 'drill' ? 
   'drag') ?
    0.2
  ) {
    notificationService.showNotification();
    return null;
} : (webStore.underlyingItems.underlying) ? 
   .value('dragData', dragDataValue, dragDataObject) || 

    (webStore.underlying 'drag'?.not? .key: Svelte.storeValues['Sling'], 'data') + 
    (Svelte.storeValues.get("Sling", "drill") ? Svelte.dataElement() : Svelte.dataElement()))
  ? 
  (Svelte store) ? 'drag' ? S
);


This component uses the document.body to create a drag item that is dragged and dropped, then it shows off the drag item on the web page.
                `;
</script>

{snippet}
                