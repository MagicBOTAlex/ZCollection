
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a basic drag and drop system.

svelte
<svelte-module name="drag-drag" module="drag" @action "move the file to the 'name' folder">
  <p>This is a simple drag and drop system.</p>
</svelte_module>

<scrip root="myModule">
  <SvelteComponent name="draggable", @action)>
    <div class="dropdown" action="action">
      <button type="date"></button> action
    </div>
  <SvelteSegment "drag"><div class="drop-down" action="action" style="text")</SvelteSegment>

<scrip root="/components/dropdown/active.txt.txt.txt.txt.txt"]
<code>
# This is a sample code block.
<p>This is a code block.</p>
<button class="dropdown">Dropdown</button>
</code>
</svelte_component>

<scrip>
  $(action "action").on("drag", "drop-down");

</scrip>

This component has three buttons. Two drag-and-drop interfaces, and one open to the right of all users.

To use this component in Svelte 5, add a Svelte block to your application:
svelte
app = new Application({});
var component = ref("myComponent", "name");
var action = "drag" (component);

This code creates an instance of the Application class and assigns it a name with two parameters.

To use Svelte 5 in Svelte SvelteKit, you need to add a SvelteKit block:
svelte
app = new Application({});
SvelteComponent = new V("myComponent", "name");
var action = "drag" (V["draggable", "drop-down"]).action;

app = Svelte("myModule", "name") < "myComponent" .class .action;

This block creates a component with two parameters.

To deploy Svelte 5 in V, open the following code:
svelte
<svelte-kissed name="myComponent" action>
  <p>This is a simple drag and drop system.</p>
</svelte_kissed>

<svelte-fragment "drag", "action">
  <p>This is a simple drag and drop system.</p>
</svelte_fragment>

<svelte-component name="myModule", action="")
  <div class="kissed" style="text">
    <button type="date"></button> action
  </div>
</svelte_kissed>

To use Svelte 5 with V, open the following code:
svelte
<svelte-component name="myComponent", @action)
<code>
# This is a sample code block.
<p>This is a code block.</p>
<button type="date">delete</button> action

<k-svelte-fragment "drag")
</k-drag>
                `;
</script>

{snippet}
                