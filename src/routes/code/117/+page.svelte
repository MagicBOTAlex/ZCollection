
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a drag-and-drop system:

svelte
import { component, ComponentAction } from "svelte-extensions";

const dragger = mount => {
    return 
    <div class="drag" draggable>{"Drag: 'Drag name'}</div>
    <input type="text" value="Name" as={component}"/>
    <button type="text" as={component} disabled={!!localStorage.name ? "New" : '' ? 'name' === 'display name' ? 1 : null || 
                'display' === 'dis' ? 1 : 2 | otherwise ? 'display' && 1}
                    value="Display" as={component}></button>
    
;



javascrip
import { v1 } from "svelte";

const component = v1("Svelte", "5");

component("drag") {
  component("dragger"),
  "Commented components",
  "Disable"
}


You can install Svelte by running the following command in your terminal:

npm bundle s7-32 ext/11


javascrip
await component().dragger(); 

const component = v1("Svelte", "5");

component("display"),
   .name,
       (name) => 
           { document.addEventListener("Display", ["display"], [display]), document ).element )


You can use Svelte to write a more complex drag and drop system.
                `;
</script>

{snippet}
                