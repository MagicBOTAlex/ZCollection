
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a drag and drop system in SvelteKit:

svelte
import { Component, transition } from SvelteKit '@svelte/kit'

const ApppageComponent = () {
  let dragOverEvent = 'drag-over'
  let dragEndFunction = 'drag-end'
  let drag = 'drag'
  let dragEndFunction = 'drag-end'
  let dragItem = 'drag item'
  let dragItemList = ['drag item list']
  let dragDropOpenMode = false
  let dragDropShow = {}
  let dragdrop = { "dragdrop" : { "text": "drag-pressing-1" } };

  if (dragOverEvent) {
    'drag-and-drop'
    transition({ duration: 500 })
      .on("name", 'myParent')
        .method('text', "Drag and drop system, please help me with the following commands")
          .action('create a new drag and drop system', { method: 'help me' })
          .when(null, { "name": "dragAndDrop" })
            .else({ { "name": "create a new drag", "name": "" } })
              .when(this !== null ? { { "text": { this !== null ? name: this }} }, { "class": this } }) : { "html": this })


Here is the Svelte component for SvelteKit 5:

svelte
import { Component, transition } from "@sveltekit/sveltekit"

const ApppageComponent = () {
  // Add drag over event to console.log
  let dragOverEvent = 'drag-over'
  let dragEndFunction = 'drag-end'
  let drag = 'drag'
  let dragItemList = ['drag item list']
  let dragItem = "drag and drop" + "" if (dragAndDrop) {
    "create a new drag and drop system"
}


This component is similar to the one above, but it adds a drag-over class to console logs.

To make Svelte components work, add this code to your SvelteKit app:

svelte
import { App, ApppageComponent } from 'svelte-kyit'
  return (module, self);

if (app.versions['v2.5'] === null) {
  sveltekit@.version4
    .addSubclass('svelte-compiler', 'svelte Kit UI v3')
  end.svelteKit
  else.svelte


If you're using SvelteKit with React, try to use the @svelte/ namespace instead.

Note that as of SvelteKit 5, the following tools are not provided by default.
                `;
</script>

{snippet}
                