
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag-and-drop system:

svelte
<scrip el relatiate-drag="drag")
relate "Drag the text field."
relatabase .text
</sveltestate>
<div class="drag" relatable=dub.io)
  <div class="drop" rel="drag"
    rel "drag and drop."
    <div class="drag" rel="drag" draggable tag "drag-and-drop system")
      <input type="text" placeholder="Enter your name" />
    tle
  </div>
  <a id="dropdown" onclick this .text
  element="javascrip" document
- element="html" document
- element="js" document
- element="css" document
</sveltecode
<div class="drag" rel="add").
</sveltestate>
<scrip el= (function self (root) { return {findElement('.context,0)),findElement('.elementList) } )
  <scrip el= " drag-and-drop.system" ]
document
< svelte.js="create")
</button
</div>
</scrip>
<svelte-external="drag-and-drop-system", rel="drag and drop"]
</svelte-internal
</scrip>
<scrip el= "drag-dodged" ]

<div id="add">Add new text field.</div>

<button type="text" onclick="javascrip document element 'add'"></button>
</svelte.state>

<scrip
rels="drag, add"
data "drag-and-drop-system"]
<div id="add">Add some new buttons.</button>


This component has two features:

1. A button with a button inside.
2. An input element with a label input.

Note: In Svelte 5, create" is used to create components, but in Svelte 4 it's not available as the feature "create" or "add" wasn't introduced into your application code, which you have at least one instance.
                `;
</script>

{snippet}
                