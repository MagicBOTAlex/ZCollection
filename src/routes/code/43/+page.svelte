
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag-and-drop system:

svelte
<svelte-dential>
  <template "defaultKeyMap">
  <button on-click="handleClick | clickOutside">Cancel</button>
</svelte-attribute-template>
<scrip type="module" interactor="component">import</scrip>

<div on-click="handleDrag | dragOver 'drag file''>Drag file</div> on-click="handleDrag" drag-over "Drag to mouse" button="click"]
<button on-click="handleClick | clickOutside">Cancel</button>

<scrip mount-variant="drag">add drag-to-dnd</scrip>

<div on-click="handleOpen | openFile | toggleButton)open file open file">
  <input type="file" accept=".svg file" value="openFile|toggleButton">
  <s vs-3>Drag and drop system</s>
</svelte-attribute>

<scrip mount-variant="drag">add drag-to-dnd</scrip>

<scrip svelte-variant="drag">add drag-to-dnd</scrip>
<scrip svelte-variant="drag"></s-attribute></s-attribute>
<scrip on-click="handleClick | clickOutside" @click="handleClick">
  <s vs="3" Drag "Drag to mouse" button]="change" button=" Click">
  forkltnah
</svelte>

<scrip mounted-variant="drag">add drag-to-dnd</scrip>


This code includes a drag-and-drop system that is added as a svelte component. It includes three SVG icons and a third file with an open file option, which can trigger the "Drag to mouse" button on click event.
                `;
</script>

{snippet}
                