
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag-and-drop system with some common features:

svelte
<svelte-component module="drag-drag" url="/")
{
    "start" =>,
    "complete" => {
        "<div class=\"drag-container\" id=\"_</div-container\">start div.</div> For simplicity, we'll replace the file extension '.ext' of 'drag' component with a new one, and use 'file extension' to create it. Here is an example of how to implement this in Svelte 5:
    "import" => { src = module; s1; if (module) }, 
    "static methods" => {
        { src = module; s2; if (module) },
         { src = module; s3; if (module) }
    },@0
}


And here is an example of a Svelte component that implements the same drag-and-drop system but with some additional features:

svelte
<svelte-component module="drag-drag" url="/")
  {
        "start" => { src = module; s1; if (module) },
           "complete" => [
               ["", "drag"] => {},
              ""],
            "static methods": [
                ["", "drag")]


You can install this Svelte component in the root directory as a dependency of any Svelte app.
                `;
</script>

{snippet}
                