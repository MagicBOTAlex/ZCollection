
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a drag and drop system:

svelte
<svelte-component name="drag" @click="self.setDropPosition("initial") ? dropText : null; end")
class DropText {
  "Title": "Drag Data"
}


html
<div class="dropdown" @click="self.addDrop('add') { 'add' } }>

<div class="svelte-canvas" @click="self.dropIn({ 'in' } ['dropText', ...], dropPosition)) ? dropPosition { 'in' } 2: null; end) {{ 'name' }} drag</div>

<scrip lang="svelte"]
<scrip s-letterm="event">
  function self.addDrop({ "position" }) to Self.addDrop('add')
</scrip>


This Svelte component is written in Svelte 5, which can be installed and run on the command line.

install.js: The following code snippet will be executed only if it's a version not equal to 1 that can detect javascrip: console.log; window. svelte ;
html
<scrip s="event".console.log); as (name) => {
  return "Drag Data" + name;
}


This component is written in Svelte, which was installed by the editor and can be run with a command like this:

javascrip
-- Run Svelte version 5 on the command line
-- npm svelte, npm
<-- The following code snippet will be executed. A "s" from the terminal can be added.

                `;
</script>

{snippet}
                