
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that provides a drag and drop system.

svelte
import { Component } from '@svelte/sapi';

@Component({
 Display = 'drop'
),
 Text = "Drag and Drop",
 Iconicon2
},


To use this component, first install the drag and drop component as a global. Then you can add it to any element like so:

html
<div class="drag-and-drop" @ draggable="drag" { draggable } { add })
  <!-- Add the drag and drop component -->
@defaultDragAndDrop

<div class="drag-and-drop">
  <p id="drag" "id" { "drift" } { "drag" }} for.</div>


The drag and drop component can be added as a global by installing it first, then you need to add the following HTML:

html
<div draggable drifter="data" { "drift" } = {
  <button class="dropdown" @click={drop} />
},
<div id="non-drag" class="{ 'drag' }}{'drag'}></div>


The drag and drop component can be added as a global by installing it first, then you need to add the following JavaScript:

javascrip
document.querySelector('#drag', 'drag).data);


You can install this component by adding it to your HTML like so:

html
<div class="non-drag" @click={drop} />
<div id="non-drag data") drag)
div draggable drifter="data", { "drift" } = {
  <button class="dropdown" @click={drop} />
},
<div draggable= "draggable" class="{ 'drag' }{'add'}"></div>


The drag and drop system can be used as a global by installing it first, then you need to add the following Svelte component code:

svelte
struct AddDrag = "Add" => 
{
  <button
    id="drift" class=""
    draggable
    text
end
@click={add}
</button>


The drag and drop system can be used as a global by installing it first, then you need to add the following Svelte component code:

svelte
import svelte.sapi;

struct AddSvelte = svelte.sapi(
  'Drag and Drop',
),
Text= "Drag and Drop",
Iconicon2
),


You can install this as a global by installing it first, then you need to add the following Svelte component code:

svelte
import { Component } from '....'];

export struct AddSvelte = svelte.sapi(
  'Drag and Drop',
);

Text= "Drag and Drop",
Iconicon2
},


To use this code as a global, first install the drag and drop system as a Svelte component:

svelte
import { Component } from '....;

export struct AddSvelte = svelte.sapi(
  'Drag and Drop',
),
Text= "Drag and Drop",
Iconicon2
},


You can add this to your HTML like so:

html
<div class="non-drag" @click={add} />
div class="drag" @click={remove}
</button>


The drag and drop system can be used as a global by installing it first, then you need to add the following Svelte component code:

svelte
import { Component } from '.....3-2-5; };

export struct AddSvelte = svelte.sapi(
  'Drag and Drop',
),
Text= "Drag and Drop",
Iconicon2
));

div class="non-drag" draggable= "drift" @click={remove}>,
<div id="drag" class="{ 'drag' }{'add'}"></div>



To use this code as a global by installing it first, then you need to add the following Svelte component code:

svelte
import { Component } from '.....3-2-5; };

export struct AddSvelte = svelte.sapi(
  'Drag and Drop',
),
Text= "Drag and Drop",
Iconicon2
));

div class="non-drag" @click={remove}>,
<div id="drag data")</button>



To install this as a global by installing it first, then you can add the following Svelte component code:

svelte
import svelte.sapi;

struct AddSvelte = svelte.sapi(
  'Drag and Drop',
),
Text= "Drag and Drop",
Iconicon2
));

div class="non-drag" @click={remove}>,
<div id="drag data")</button>],



To use this code as a global by installing it first, then you need to add the following Svelte component code:

svelte
import { Component } from '.....4-2.5; };

export struct AddSvelte = svelte.sapi(
  'Drag and Drop',
),
Text= "Drag and Drop",
Iconicon2
));

div class="non-drag" @click={remove}>,
<div id="drag data")</button>],



This code example provides a drag and drop system that can be used as a global by installing it first, then you need to add the following Svelte component code.
                `;
</script>

{snippet}
                