
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component for a drag and drop system. This component will have some features like copying files, moving HTML elements, adding JavaScript code snippet to files.

svelte
<svelte-component name="DragDrop" @appApp {
  add("drag", "Copy")
  add("drop", "Move file")
  add("add-javascrip", "Add JavaScript code snippet", function (title) { self.title; })
  add("remove-javascrip", function (self) {
    if (!this.element.queryMap.contains(this.element)).name && !this.element.queryMap.contains(this.element).name) return 'javascrip' === this.element.name
    else
    .not(this.element.name, "javascrip")
    .else
    }
}


svelte
<svelte-component name="HTMLElement" @appApp {
  self.html().scrip('add-attribute')
};

This component has the following features:

* Drag to copy files
* Drop to move a file.
* Add to add JavaScript code snippets

The components can be attached like this:
                `;
</script>

{snippet}
                