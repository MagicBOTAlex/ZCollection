
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component to help illustrate how to create a drag and drop system.

Firstly, let's define some variables that we will use in our component.
typescrip
import { screen } from '@svelte-three/core';
import { appStyleColor } from 'react-ansi-custom-borders-styles';

const layout = {
  classes: [
    // "grid", 
    'row', 
    'gridRow' ]
};
const styles = {
  classNames: [
    // "grid", 
    'gridColor', 
    'gridBorderColor'
  ].join()
};

export default (svelte.Component);

                `;
</script>

{snippet}
                