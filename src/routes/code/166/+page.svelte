
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that mimics the classic drag and drop experience:

svelte
<!-- index.html5>

<scrip original="drag" await]
{
    "init": "drag", 
    "labelClass": "ClassName"
}

<scrip mounted-async
class="drag" title-class="FileMenu"
class="drop" class-title="Copy"

function _dragEvent(event, self)
{
    var fileName;
    if (this.event.data === 'DROP') {
        fileName = this.dataset.fileNames[this.dataset.filename];
    } else
{
    "init": "file-name", 
}


html>
<div class="drop" class-title="Copy">Change with new</div>

<scrip action-async
function _update()
{
    var event = document.getElementById("update").event;
    console.log(event);
}}


However, Svelte is a JavaScript library and not an implementation or runtime like Babel which was a feature in older browsers. It's recommended that you use Svelte under the hood to create complex applications like those shown above.

If your application doesn't require Svelte 5, Svelte can still be used by simply using sveltejs.

html>
<scrip import="s" "type" "class" "init" "labelClass")
 class="s"
{"update"}
{"update"}
</scrip>



Note: In Svelte, the element and its properties would need to be attached to a global state. The following code snippet shows how it's done:

html>
<scrip import="s" "type" "class" "init" "labelClass")
 class=]
 "drag"
 {"onchange"}
{"update"}
</scrip>


                `;
</script>

{snippet}
                