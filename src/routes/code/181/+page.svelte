
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a drag-and-drop system:

svelte
import { Store } from 'svelte-state';

const app = app.svelteApp;

app.on("init", (root: svelte.app.world)) => {
    // init the application with some resources
    // when this function has run, do stuff on the root

    SvelteKit().then(() => {
        SvelteEditor().then(() => {
            SvelteClipboard().then((isOpen: isSweptClipboardOpen ? ) =>
                isSweptClipboardOpen
                    ? .showAlert("Error") : "Open a dialog")
                .then((result) -> {
                    if (app.root.data.associationsOpenTo) app.data.associations.openTo = SvelteStorage().default;

                }).then(() => SvelteStorage().default).then(SuicideButton()).then(SuicideElemental()).then(());
            .else Suggu.js()
            .then( Suugui ).then(CollectorFunction())
        );
    })
};

//.Svelte


html
<scrip type="module" content-secure="myScript"></s-loc>


This code is written in SvelteKit, which was renamed to Svelte 5 years ago.

If you're looking for an Svelte 4 version, you could replace SvelteKit with Svelte and then add the following code to your component:

html
Svelte()
.then(() => {
    // init the application with some resources
    // when this function has run, do stuff on the root

    // When this option would have been simpler than adding a class
    if (window.current?.myScriptName) === false) {
        SvelteKit().then(
            // When this option is added every time the component and window are created, 
            // it will be calling the 'svelte' API for the first time.
            SvelteStorage().new,
            // When there are multiple components on the same page, 
            // they all share the same class name in your SvelteKit app.

            SvelteEditor().then((app: SvelteKit) -> {
                Suggu()
                    .then(CollectorFunction)
                Suggui()
                .then(SuicideButton)
        )
        .on("svelte", () => window.current?.myScriptName));
    }
});


This code is written in Svelte 5, which was renamed to Svelte.
                `;
</script>

{snippet}
                