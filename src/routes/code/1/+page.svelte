
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component for a drag and drop system. This component will simulate the functionality of Svelte 5, which allows you to drag-and-drop (Dn) objects in SvelteKit.

svelte
import { View, Text } from '@svelte-kit/app';
import { Component, ComponentKit, Kit } from "@svelte-portal/kit";

const App = (root) => {
  class AppComponent() {
    @ ref("draggableElement");
    @ ref("dragItemElement");
    @ ref("loadingElement");
    @ ref("footerElement");

    addRef('class1ClassElement);

    onTabClick([ref]) -> "Open the console menu" {
      this.dn("drag), 'Add drag feature", ["Drag"], "a new class")
        .call("addClassTo) add me to a specific class")
        .intoComponent("class)
      button.open()
      .when(this)
    button click("Start")
  }

};


This component has the following properties:

* ref: The reference object.
* drag and addClassTo
+ open and can open.
- Clicked
+ button clicked.0, where & "Add".
                `;
</script>

{snippet}
                