
                <script>
	const snippet = String.raw`                Here is a basic example of how to implement a Svelte 5-based drag and drop system.

Firstly, let's start by implementing the component:

svelte
<svelte-component module="draggable" action="add" . "remove" action="drag" action="change"></svelte>


To use this component in an Svelte 5 environment:

html
<svelte-component module="draggable" action="add" action="drag" . "remove" action="change">
  <button>Click me</button>
(button) to drag and drop</button>


In this Svelte 5-based implementation:

* The component is named sveltecomponent.
* It uses the module attribute to specify the Svelte module name. draggable. This module has two properties: action, which represents the action being executed, and .remove: a function that returns either or one value depending on whether this is the first execution of the Svelte component.

For Svelte 5 implementation:

svelte
<svelte-component module="add" action="drag" . "change"
  <button>Drag & Drop system</button>
<button>Click me</button>

(button) to implement drag and drop system in an Svelte 5 environment

<svelte-component module="add" action="remove" . "drag" action="drag"></svelte


The key points:

- The component is named SvelteComponent.
- It uses the module attribute to specify the Svelte module name. draggable. This module has two properties: action, which represents the action being executed, and .remove: a function that returns other value if this is not the first execution.
- The component implementation:
    html
<svelte-component module="draggable" action="add" . "remove" action="drag" action="change"></svelte>


To use Svelte 5:

html
<svelte-component module="dragger" action="update" . "upgrade" action="create" action="delete"]
<button>Drag & Drop</Button>
<button>Drag & Drop system</Button>

<svelte-component(module), actions, "drag", . "replace" action)


To implement Svelte 5:

svelte
<svelte-component module="dragger" action="update" . "add"
  <button>Drag and drop system</Button>
<button draggable="remove">Click me</Button>

<svelte-component(module) actions, "drag", . "change")
<button>Drag & Drop</Button>
<button>Drag & Drop system</Button>


In this Svelte 5-based implementation:

* The component is named SvelteComponent.
* It uses the module attribute to specify the Svelte module name.
* The component has two properties:
+ actions: a svelte.kwargu, which represents the action being executed, and .remove.underlineAction if it was not a drag & drop. action is "create" or retrieve with dragger if that's not the same case as Svelte 5 implementation.
+ There are two actions in this component.

To implement Svelte 5:

svelte
<svelte-component module="draggable", action="update, 'remove' . 'drag' . action"
  <button>Drag and drop system</Button>
<button>Drag & Drop</Button>

<svelte-component(module), actions, "replace" if not, . "modify" action)


To implement Svelte 5:

svelte
<svelte-component module="draggable", action="update, 'add' 'remove' . 'drag' . action"
  <button>
 Drag and drop system in an Svelte 5 environment<brate>Drag & Drop</Button>
<end Button></Button>

<svelte-component(module), actions, "replace" if not, . "modify" action)


The key points:

- The component is named SvelteComponent.
- It has two properties: actions, which is a function that can be either or one value depending on whether this is the first execution.
+ This component implementation:
    html
<svelte-component module="dragger", "update, 'add' . 'rem', . . 'drag' . action]
<svelte-component(module).action)
<Button>Drag and drop</Button>
<svelte-component( SvelteComponent.module.undown.drag).action).remove().remove("add".).add

                `;
</script>

{snippet}
                