
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component for a drag and drop system. This example will use Svelte-keatys to create the component.

svelte
import App from '@svelte/kem/2d/section-3';
import state from './state.js";
import FileList from '@svelte/match/matched/5;
import draggingList from '@svelte/dragging-list';
import dragList from 'drag.json';

let state = { ...state, 4: [1, "Drag", 2], 6: [] };

let initState = { drag: true, fileSelect: false };
let dragState = {
    drag: true,
    fileSelect: {}
};


svelte
import selfBoundDragger from '@svelte/dragging-list';
import draggingList from 'drag.json';

selfBoundDrag('componentName', {
  drag: true,
  fileSelect: {}
});


This Svelte component demonstrates the following features in Svelte:

* self-bound means "the component is bound to a single value".
* svelte-inlet has @svelte/ bindings.
                `;
</script>

{snippet}
                