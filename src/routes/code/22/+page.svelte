
                <script>
	const snippet = String.raw`                Here is a basic implementation of a Svelte component that provides a drag and drop experience. This example assumes you have already set up the components in a way that they can be used with Svelte.

svelte
from Svelte3 import DOMPUS, file;

let ComponentName = "Drag";
let dragComponentName = 'file';
if (dragComponentName) {
    component dragComponentName;
} else if (Array.prototype.slice.callTo)
  . join(1, 0)) {
    write$. async load
        > ("drag", { name: "File" })
      else
        $ (document).ready(function()
        {
            // Open a new file.
            var file = File.open('for', 'file));

            // Write the current state of the component to the server.

            return file;

        });

    write$. called ($);
}
                `;
</script>

{snippet}
                