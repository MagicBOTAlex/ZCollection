
                <script>
	const snippet = String.raw`                Here is a Svelte component that demonstrates a drag-and-drop system:
dart
// main.js file
class Main {
  createDiv() {
    return document.createElement("div", { id: "1" });
  }

  div="div", {'id":"1"}, "Drag and Drop";
}

// main.js file (Svelte 3, but not Svelte 4)
svelte Elm.mainDnd = {
  'createDiv': (div) => {
    return document.createElement("div", { id: "mainEl" });
  },
};

javascrip
// create a Svelte app
const app = svelte.app.inName;

// create a div element with the drag and drop system
app.under.div("Drag and Drop").createDiv();

// render the created div element
 Elm(mainDnd, "Main", { mainEl: "div" }, mainEl).render();

To implement this Svelte component in an existing project, you would need to install Svelte on your server. Then you can use it to create a new Svelte app:

javascrip
svelte.app.inName = 'my-app';

// open the app in browser
if (app.is("App.js", { name: "Main" })) {
  app.createDiv((div, id) => {
    // render your code here
  ).render();
}


To use Svelte 5 on your app:

javascrip
svelte.config.svelteApp = svelte.app.inName;

// open the app in browser (you can also add a command to run a command)
if (app.is("App.js", { name: "Main" })) {
  // render your code here
  mainApp.svelteApp.createDiv((div, id) => {
    // use this command or add to run commands
    mainApp.svelteApp.createDiv(0).render();
});


To install Svelte on your server:

bash
sudo sitlettify -install=apt5.1 -source=s
sudo apt-install=all -package s


You can use it as follows:

javascrip
svelte.app.inName = 'my-app';

// open the app in browser (you can also add a command to run a command)
if (app.is("App.js", { name: "Main" })) {
  // render your code here
  mainApp.sutter().createDiv(0,1).render();
}


To install Svelte on an existing project:

bash
sudo apt-add=update -command


With this command you can open the app in browser like so:
bash
sudo svelte-Install -install=apt3.0-s5 "svelte"

You can use it to create a new Svelte app:

javascrip
svelte.app.inName = 'my-app';

// open the app in browser (you can install Svelte on your server)
if (app.is("App.js", { name: "Main" })) {
  mainApp.sutter().createDiv(0).render();
}


To use it as a command:

bash
sudo svelte-Install -install=apt3.0-s5 "s"

Your Svelte app is at S://app-name.
                `;
</script>

{snippet}
                