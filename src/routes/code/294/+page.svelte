
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a basic drag and drop system:

svelte
<scrip lang="s">kangajah</scrip>
<sveltekit-app "Drag and Drop System" { "Click me!" }}


And here is the implementation of the component using JavaScript and the HTML element <app>, which is included by default in Svelte apps.

html
App<scrip scrip="drag" {"key": 1} {"text": "Drop it!"]></scrip>


javascrip
import { View, Widget } from './widgets';
import { Clipboard } from 'window/clipboard';

// Handle click event

document.querySelectorAll(".app").on("click", (event) => {
    if (this.classList.contains('drag')) {
        return this;
    }

    var container = document.createElement(this.name());
    if (container && container.children[0].classList) {
        container.remove().then(function() {
            clipboard = 'copy' + container
            clipboard = this
            window.clipboard = clip
            clip.readonly()
            click();
        }
        container.close();
        return container;
    }

    return new container();
});

// Create drag components

const dragComponentName = "drag";
const dragOpacity = 4;

document.drag(1).on("change", (event) => {
  window.clipboard = 'copy' + document.write(dragOpacity, "Copy")
});

                `;
</script>

{snippet}
                