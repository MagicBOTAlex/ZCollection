
                <script>
	const snippet = String.raw`                Here is a basic Svelte component for a drag and drop system.

dart
// dr_ko.dart

import "module".dart;

// A simple text file.
// dart() {
//   return DartObject.new_string("Hello\nWorld", "Copy", "Shift");
// }
new_functiondart('textfile').then('New Svelte Component')
               .select(function => 'Main Svelte Component.')
               .bind(this)
               .add dart('Svelte component')
                 ->on(2,1).then(3,1).then[0]
                  ->call(this,3,1),this;
        var sclass = this.else('main).if(this.else(0));
         return sclass;
    }


And here is Svelte 5 code for it.

s
// dr_ko Dart module and object

import "module";

var sclass = {
  'Main' -> 'Svelte component' -> 'Call',3,1,
  'This'-> 'else'-> 'Main';
};


To use this Svelte 5 code, you would add the following code to your .dart module.

dart
// . dart() 
dart(2550) {
  var sclass = {
    'Main' -> 'Svelte component' -> 'Call',3,1,
    'This'
    }
  'this'-> 's'-> 'Main';
};


Or to use Svelte module.

s
// . dart() 
dart(2550) {
  var sclass = {
    'Main' -> 'Svelte component' -> 'Call',3,1,
    'This'
  };
  if (sclass.verbs().at('this').-> @0)
   return sclass;
};


To use Svelte module, you would add the following code to your .dart module.

dart
// . dart() 
dart(2550) {
  var class = 'Svelte component';
  if (sclass.verbs().at('verb'),3,1)
   return DartObject.new_string(class,"Main", "Shift");
};


To integrate Svelte module and object.
                `;
</script>

{snippet}
                