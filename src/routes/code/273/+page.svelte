
                <script>
	const snippet = String.raw`                Here is an example of how you might create a Svelte component for a simple drag-and-drop system.

svelte
<div class="dropbox" @click={handleClick} on:DragDropItem clicked="true">
  <h2 id="title"> Title</h2>
</div>

<scrip init => {isDragging = isElement('title') ? 'dragging' : '') => {}}>
  {
    click
}

This code defines two classes in the Svelte component. The first class isDrag creates an element with ID title and assigns a new value to is dragging. If this condition matches, then assign a new value of isdragging or click as the second child of div. 

The second Svelte component does not do anything.

svelte
<scrip init = { isDragging } 'not drag' (2) >> { click = 'not clicking' }

<div class="dropbox" @click={handleClick} on:DragDropItem clicked='true'></scrip> 
<div class="notdrag" @click=handleClick></div>

<div id="title"> Title</div>)


This Svelte component is a bit more complicated than the first one. The  click = and @click={handleClick} components can be attached to any Svelte element by adding the following property, isdragging, or click.
                `;
</script>

{snippet}
                