
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that mimics the features of a "drag and drop" system.

This component has several features and uses some Svelte-specific methods to interact with it.

svelte
import SvelteKit from './kit';

const div = S SVG <div id="dropArea" class="droparea" onclick="updateDropArea()">Drag and Drop</span>;
Svelte.half(()->) { SvelteKit.map(); }
Svelte.update(div, 1);


svelte
const update = ref 'drag-end-0';
if (isT(update))
 | .project('change text', id)
 | .head("Drag") + "Drop" - 'name' |
 |   "drop" - 'text' | 
 |    "update" | { $url: "$rootUrl"} }
| console .project
 |  .key("drag",1,0), {
            title = "Update Text",
            descripion = "This is a red square.",
        }


gasmascrip
// Svelte component for dragging and dropping
function createComponent(name) 
    var parent = "HTML Element"
    if (name === "HTML")
       console.log name

    return name; 


The code above creates an Svelte component that is currently being used by the user.
                `;
</script>

{snippet}
                