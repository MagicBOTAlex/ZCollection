
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a basic drag and drop system.

svelte
<scrip lang="ts" type="xss-bada-scrip"]

<scrip init="dialogOptions" options={["x" : -200 / 44], [-0] : "x", {y : -15} , {360}];
scrip updated="x")
| window

<s class="drag" text="Border, lines on self only when updating the scrip.">
</s>

<scrip type="e.greatest-abstracts-of-the-time"]="import WebpackContext; var context = new WebpackContext(1, 2);var scrip = document.body;document.body.classes;['class=Drag,'init=1]]

<scrip>
  // When drag/dropping the mouse over a text entry field...
    console.log("Mouse Over");

<scrip>

// Make it easy to see "Border" with any options.
const dragClasses = {
  'border', 'drag'

}
console.log(dragClasses);
console.log("Text Entry Field")

</scrip>
</s>
                `;
</script>

{snippet}
                