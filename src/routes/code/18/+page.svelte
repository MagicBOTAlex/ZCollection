
                <script>
	const snippet = String.raw`                Here is an example of how to create a Svelte 5-based drag and drop system:

dart
// app.js

import "@svelte-with-history"; // import @Svelte@History from @svelte-to-horizon

var app = new App()

// App component properties
var browser = App.createBrowser("Asian");

App.listen("Mouse) {
  browser.listen("Mouse", app.state.mouse, this);
}

// App component
new Tab() {
  Title();
}


javascrip
// svelte.js

import { SvelteKitSvelteComponent } from SvelteKitS;
import { SvelteKitS } from SvelteKit;

export function DragComponent() {
  SveltekitS("Drag").add(
    (title) => (this.title),
    title,
);

new Tab() {
  Title();
};


gml
// svelte.html

<sveltekit.js/1](window.close as [Window] { "Drag" } .title)


gml; svelte.js

<canvas ref="change">>
  drag
</canvas>

; <sveltekit.0("add") => SveltekitS()("add", app.state.mouse, self)>)

return (
  SveltekitS("drag"),
  "Drag",
);

new Tab()

{
  Title();
}
;


This is a basic example of how to create a Svelte component with a drag and drop system.
                `;
</script>

{snippet}
                