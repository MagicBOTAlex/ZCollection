
                <script>
	const snippet = String.raw`                Here is an example of a Svelte component that implements a basic drag and drop system.

svelte
<svelte-component name="drag-and-drop-system" @click="onClick 'Drag and drop system'">

    <input type="text" value="drag" on-screen click="on Click dragging | Drop the text below, run AddActive in the browser.">
    
</svelte>

<scrip,uselist>

<scrip>
-export = "drag-and-drop-system";
import element from './drag.js' => element;

// Use
update this component to create a drag-dnd system

// Example usage
svelte
on Click= {
  'select= "Add' 'Drag the text below' +
    "run Add
'
}
</svelte>
</scrip>

<scrip root>
export var dragAndDropSystem= (s) -> s.map('drag');


This component is a simple Svelte component with some basic state management. The dragAndDropSystem component has two states, one that represents the text "Add", and the other that represents the drag of the text "Drag".
                `;
</script>

{snippet}
                