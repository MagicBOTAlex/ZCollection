
                <script>
	const snippet = String.raw`                Here is a Svelte component for a drag and drop system.

dart
// Computed property in the component

class Draggable {
  String type = "Drag";
}

// Action creator that can trigger events
svelte.xit('action: when you run code', () -> {
  // Simple drag, it's called Drag by Svelte 5.
  $.add('drag-1', (move) -> {
    console.log("Drag", "2");
    console.log("Drag", "3");
    console.log("Drag", "4");
    console.log("Drag", "5", "$.args", ["Drag" + $.result("drag-name", ).result(0, 1)]), "$.args"
      $add('drag-6', (move) -> {
        // Drop on hover.
        $add('drop-on-hs-box-2', (move) -> {
          console.log("Drop on", "3");
        }, "actions";
        console.log("Drop on", "4");
        console.log("Drop on", "5");
        console.log("Drop on", "6");
      })
    $add('drop-on-hs-box-1', (move) -> {
        console.log("Drop on", "2");
        console.log("Drop on", "3");

        // Drag and drop.
        Svelte.block(2).commit;
          .action
            'drag-7',
  });
}

// Action creator that can trigger events

svelte.xit('action: when you run code', (action) -> {
  console.log("$.args", "$add");

    $add("log-box", "actions")
      .dropOnHsBox = Svelte.block(2).commit;
        '.action
          'drag-8',
        Svelte.block(7).add,
          .action("drag-9"),
            action($$.result("actions", $add), "$.result(0, 1)", function() {
              console.log("Drop on", "5");
              console.log("Drag", "6")
              return .action("Drag") === Svelte.block(2).add("drag", 1) ? $.result("drag-name", 1, function(), "action"), "S$$.result("action"), "S$".add("actions").add("block-4");
                console.log($$.result("drag-name", "Drag", "1") + "$$.result('action', $add), S$.result('block-2', 'S'.result("actions", .result(0, 1)), function(), "action"), "S" .$().result("actions", .result(0, 1))).add(6);
                console.log($$.result("block-4", 'S').add(5), "$.result('action', $add),
                    ".S".result("block-2", "S"))
                    ? .add(7, function(), S
                        action.$add("Drag", 1)
                    )
                  .action()
                .action(
          '$.result("drag-name", "Drag", "2")',
          S$.result("block-4", "S").add(5),
          ".action"
        )
    }
});

// Action creator that can trigger events

svelte.xit('action: when you run code', (action) -> {
  console.log("$/.result(actions, .result(0, 1))") + S$.result("block-4", "S").add(5),
          $.action($$.result("actions, block-2", .result(0, 1)), "S".result("block-4"), "S" .$().result("action)", function(), "action"),
                  ".drag",
                  S$.result("action", .action());
});

// Action creator that can trigger events
svelte.xit('action: when you run code', (action) -> {
  console.log("$/.result(actions, .result(0, 1))") + S$.result("block-4", "S").add(5),
             ".drag",
             S$.result("action", .action());
});


This component demonstrates how to use the following Svelte components:

* svelte.com('action', action) - This line is used in many places in your application. It's called when an action occurred.

dart
// Action creator that can trigger events
svelte.xit('action: when you run code', (action) -> {
  console.log("$/.result(actions, .result(0, 1))") + S$.result("block-4", "S").add(5), "$.result(actions, .result(0, 2)), function(), "action")
              .action();
});


This component demonstrates how to use the following Svelte components:

* svelte.com('action: when you run code', action). This is called when an action occurred.

dart
// Action creator that can trigger events
svelte.xit('action: when you run code', (action) -> {
  console.log("$/.result(actions, .result(0, 1))") + S$.result("block-4", "S").add(5), "$.result(actions, .result(0, 2)), function(), action
               .action()
});


This component is the same as the previous one. It's just a simple drag and drop system that can trigger actions.

Note: Svelte 5 has removed the ability to add multiple add components from Svelte 4's block creation features.
                `;
</script>

{snippet}
                