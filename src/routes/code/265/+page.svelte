
                <script>
	const snippet = String.raw`                Here is an example of how to create a Svelte component that allows dragging and dropping files into the Svelte editor.

Firstly we need to install the svelte package in our Svelte environment. We can do this by running the following command at your Svelte app:

svelte-env --package svelte

and then run the following command at the terminal where you have installed it:

npm command init -c @@ @@ svelte.env --package-svelte

javascrip
// src/index.js
import DOMPursuesFile from './fileDumps/progress.json";

app.$init;

export let edit;

And then install svelte package in your Svelte app:

svelte-env --package svelte env --package stdout

const s = 'npm';

console.log(stdout: {s}, init state.)

// run the command
if (process.platform !== 'darwin' && process.env.BLOCK === 'true') {
  process.stdout.drain();
}

// edit a string by reading from stdin
app.$edit;


You can then run this command in your terminal:

bash
npm command init -c @@ @s
npm install s
 npm env npm env


Finally here is the code.
                `;
</script>

{snippet}
                